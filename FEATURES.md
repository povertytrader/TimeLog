# TimeLog — Features Overview

A lightweight, single-file time tracking app that runs locally in your browser. No account, no cloud, no subscription.

---

## ⚡ Quick Entry (Ctrl+Alt+T)
Select any text in Outlook, Teams, or your browser, press the shortcut, and a compact popup opens with the text pre-filled as the task description. Fill in Client, Project, Task, and hours — done in seconds.

- Validates all required fields before saving
- Press **Enter** to Save & Close
- **Open App** button to jump to the full app from the popup

---

## 📋 New Entry
Log time entries with:
- **Date** and **Hours**
- **Client**, **Project**, **Task** (searchable dropdowns with auto-add)
- **Task Description** (free text, pre-filled from quick entry)

The last **6 recent entries** are shown below the form with edit ✏️ and delete 🗑️ options.

---

## 📅 Weekly / Daily View
- Toggle between **Week** and **Day** view
- **Today** button to jump to current day instantly
- Pick any specific date with the date picker
- Navigate with Prev / Next buttons
- Sort entries by Date, Client, Hours, or Task
- Each entry shows: `CLIENT.TASK DESCRIPTION` for quick scanning

---

## 📊 Report
- View entries by week or all at once
- Filter by Client, Project, or Task
- Full-text search across all fields
- Dates displayed in compact single-line format (e.g. `Wed, Feb 18`)
- **Copy to Clipboard** (tab-separated, ready to paste in Excel)
- **Export CSV**

---

## 🏖️ Upcoming Holidays
Dashboard shows the next upcoming public holidays for USA and EMEA regions, fetched live from [Nager.Date](https://date.nager.at/).

---

## ⚙️ Manage
Add, view, and remove **Clients**, **Projects**, and **Tasks** from your lookup lists. Export or import all data as JSON for backup.

---

## 💾 Data & Privacy
All data is stored locally in your browser's **localStorage** — nothing leaves your machine. Export your data anytime as a JSON backup.

---

## 🖥️ Install as Desktop App (PWA)
Install directly from Chrome or Edge — the app gets its own window, taskbar icon, and can launch on startup. No installer needed.

---

## 🌙 Dark Mode
Toggle between light and dark themes. Preference is saved automatically.
